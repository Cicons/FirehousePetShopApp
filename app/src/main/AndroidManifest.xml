<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cs246.firehousepetshopapp">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <!--<uses-permission android:name="android.permission.GET_ACCOUNTS" />-->
    <!--<uses-permission android:name="android.permission.READ_PROFILE" />-->
    <!--<uses-permission android:name="android.permission.READ_CONTACTS" />-->
<uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SignInActivity"
            android:label="Sign In">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CreateUserActivity"
            android:label="Create User">
        </activity>
        <activity
            android:name=".CustomerMainActivity"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name=".HelpRequestActivity"
            android:label="Help Request">
        </activity>
        <activity
            android:name=".ManagerMainActivity"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name=".ManageRewardsActivity"
            android:label="Manage Rewards">
        </activity>
        <activity
            android:name=".ViewHelpRequestActivity"
            android:label="View Help Requests">
        </activity>
        <activity
            android:name=".AdjustPointsActivity"
            android:label="Adjust Points">
        </activity>

        <!--<activity-->
            <!--android:name=".LoginActivity"-->
            <!--android:label="@string/title_activity_login">-->
            <!--<intent-filter>-->
                <!--<action android:name="android.intent.action.VIEW" />-->

            <!--</intent-filter>-->
        <!--</activity>-->
    </application>

</manifest>